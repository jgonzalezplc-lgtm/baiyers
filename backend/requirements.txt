fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.1
supabase>=2.9.0
httpx>=0.27.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
apscheduler>=3.10.0
beautifulsoup4>=4.12.0
anthropic>=0.115.0
# Import/export de proveedores por Excel
pandas>=2.2.0
openpyxl>=3.1.0
xlrd>=2.0.1
# IA (Gemini) e integración Gmail
google-generativeai>=0.8.0
google-api-python-client>=2.190.0
google-auth>=2.55.0
google-auth-oauthlib>=1.4.0
# MCP server y JWT (se importan al arrancar main.py)
# Pin exacto: Railway reutilizó una capa antigua/incompleta llamada `mcp` que
# no incluía mcp.server.fastmcp. Esta es la distribución oficial verificada.
mcp==1.28.1
python-jose[cryptography]>=3.3.0
# PDF de la Orden de Compra desde el backend, para clientes headless (MCP) que
# no pueden renderizar `frontend/components/OCPDFTemplate.tsx`. fpdf2 es Python
# puro: no agrega binarios de sistema al build de Railway.
fpdf2>=2.7.0
